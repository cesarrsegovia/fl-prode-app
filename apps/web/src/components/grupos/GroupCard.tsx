'use client';

export function GroupCard() {
  return (
    <div
      className="rounded-xl p-4"
      style={{ background: 'var(--surface)' }}
    >
      {/* TODO: Group name, member count, position badge */}
    </div>
  );
}

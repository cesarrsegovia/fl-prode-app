'use client';

export function MatchCard() {
  return (
    <div
      className="rounded-xl p-4"
      style={{ background: 'var(--surface)' }}
    >
      {/* TODO: Home team vs Away team, 1/X/2 selector, optional exact score */}
    </div>
  );
}

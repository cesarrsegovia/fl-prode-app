export default function ProdeFechaPage({
  params,
}: {
  params: Promise<{ fechaId: string }>;
}) {
  return (
    <div>
      <h1 className="text-2xl font-bold">Pronósticos</h1>
      {/* TODO: ProdeForm with MatchCard list */}
    </div>
  );
}

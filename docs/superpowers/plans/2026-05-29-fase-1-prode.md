# Fase 1 — Dominio Prode — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrar el dominio Prode (6 componentes + 3 páginas) al sistema de diseño unificado, hacerlo responsive mobile-first (360px) y accesible, en una sola pasada por archivo, adoptando los primitivos creados en la Fase 0.

**Architecture:** Una pasada por archivo: migrar tokens legacy→nuevos + arreglar responsive + agregar a11y. Se adoptan los primitivos de Fase 0 (`PillTabs`, `PercentBar`) y se extrae un hook compartido `useFixtureWithPredictions` para eliminar la duplicación del fetch entre las dos páginas de fecha. NO se toca el bug funcional del filtro de Prode (Fase 7) ni se agregan features.

**Tech Stack:** Next.js 15 + React 19 + Tailwind v4 + TypeScript + next-intl + vitest. Branch: `ui/fase-1-prode` (creada desde `main`).

**Idioma:** Comunicación con el usuario en español. Commits en español (convención del repo).

---

## Convenciones (heredadas de Fase 0)

- **Tokens nuevos** (destino): `surface`/`surface-1/2/3`, `foreground`, `ink-muted`/`ink-dim`, `neon` + `primary-foreground`, `destructive`, `line`/`line-strong`, `citrus`, `grass`. **Legacy a eliminar en estos archivos:** `surface-container-*`, `on-surface-variant`, `text-white`→`text-foreground`, `text-black`→`text-primary-foreground`, `bg-primary text-black`→`bg-neon text-primary-foreground`, `text-primary`→`text-neon`, `text-red-400`→`text-destructive`, `var(--accent-primary)`→`text-neon`.
- **Tests:** vitest config es `apps/web/vitest.config.mts`. Lógica pura/hooks con TDD (`*.test.ts`; hooks llevan `// @vitest-environment jsdom` en la primera línea). Componentes/páginas se verifican con `pnpm build` (typecheck estricto) + revisión visual final.
- **NO usar `pnpm lint`** (este repo no tiene config de ESLint; `next lint` cuelga en prompt interactivo).
- **Comandos (desde `apps/web`):** test puntual `pnpm exec vitest run <ruta>`; toda la suite `pnpm test`; typecheck/build `pnpm build`.
- **Primitivos de Fase 0 disponibles:** `@/components/ui/pill-tabs` (`PillTabs`, `PillTab<T>`), `@/components/ui/percent-bar` (`PercentBar`), `@/components/ui/async-section` (`AsyncSection`), `@/hooks/useFetch`, `@/lib/date` (`formatDeadline`, `formatMatchDate`, presets).

## File Structure

**Crear:**
- `apps/web/src/hooks/useFixtureWithPredictions.ts` — hook compartido (fixture + predicciones + loading/error)
- `apps/web/src/hooks/useFixtureWithPredictions.test.ts`

**Modificar:**
- `apps/web/src/messages/{es,en,de,fr}/prode.json` — keys de a11y para MatchCard
- `apps/web/src/components/prode/Countdown.tsx` — tokens
- `apps/web/src/components/prode/MatchCard.tsx` — responsive + a11y
- `apps/web/src/components/prode/PredictionsProgressCard.tsx` — adoptar `PercentBar`
- `apps/web/src/components/prode/PredictionsFilterTabs.tsx` — adoptar `PillTabs`
- `apps/web/src/components/prode/ProdeForm.tsx` — tokens + a11y + `PercentBar`
- `apps/web/src/app/(main)/prode/page.tsx` — enum `MatchStatus` + `lib/date`
- `apps/web/src/app/(main)/prode/[fechaId]/page.tsx` — tokens + hook + a11y
- `apps/web/src/app/(main)/prode/[fechaId]/resultados/page.tsx` — hook + responsive + empty

**Sin cambios (ya cumple):** `FeaturedPickCard.tsx` (tokens nuevos, truncado OK).

---

## Task 1: Hook `useFixtureWithPredictions` (TDD con jsdom)

Elimina el `useEffect`+`Promise.all` idéntico de `prode/[fechaId]/page.tsx` y `resultados/page.tsx`.

**Files:**
- Create: `apps/web/src/hooks/useFixtureWithPredictions.ts`
- Test: `apps/web/src/hooks/useFixtureWithPredictions.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// @vitest-environment jsdom
import { describe, expect, it, vi, beforeEach } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';

vi.mock('@/lib/endpoints', () => ({
  fixtures: { one: vi.fn() },
  pronosticos: { byFixture: vi.fn() },
}));

import { fixtures, pronosticos } from '@/lib/endpoints';
import { useFixtureWithPredictions } from './useFixtureWithPredictions';

const fx = { id: 'f1', round: 1, matches: [], closeAt: '2026-06-01T00:00:00Z' };

beforeEach(() => {
  vi.clearAllMocks();
});

describe('useFixtureWithPredictions', () => {
  it('carga fixture y predicciones', async () => {
    (fixtures.one as ReturnType<typeof vi.fn>).mockResolvedValue(fx);
    (pronosticos.byFixture as ReturnType<typeof vi.fn>).mockResolvedValue([{ matchId: 'm1' }]);

    const { result } = renderHook(() => useFixtureWithPredictions('f1'));
    expect(result.current.isLoading).toBe(true);
    await waitFor(() => expect(result.current.isLoading).toBe(false));
    expect(result.current.fixture).toEqual(fx);
    expect(result.current.predictions).toEqual([{ matchId: 'm1' }]);
    expect(result.current.error).toBeNull();
  });

  it('tolera fallo de predicciones devolviendo lista vacía', async () => {
    (fixtures.one as ReturnType<typeof vi.fn>).mockResolvedValue(fx);
    (pronosticos.byFixture as ReturnType<typeof vi.fn>).mockRejectedValue(new Error('no preds'));

    const { result } = renderHook(() => useFixtureWithPredictions('f1'));
    await waitFor(() => expect(result.current.isLoading).toBe(false));
    expect(result.current.fixture).toEqual(fx);
    expect(result.current.predictions).toEqual([]);
    expect(result.current.error).toBeNull();
  });

  it('expone error si falla el fixture', async () => {
    (fixtures.one as ReturnType<typeof vi.fn>).mockRejectedValue(new Error('boom'));
    (pronosticos.byFixture as ReturnType<typeof vi.fn>).mockResolvedValue([]);

    const { result } = renderHook(() => useFixtureWithPredictions('f1'));
    await waitFor(() => expect(result.current.isLoading).toBe(false));
    expect(result.current.fixture).toBeNull();
    expect(result.current.error).toBeInstanceOf(Error);
  });
});
```

- [ ] **Step 2: Run to confirm failure**

Run (desde apps/web): `pnpm exec vitest run src/hooks/useFixtureWithPredictions.test.ts`
Expected: FAIL ("Failed to resolve import './useFixtureWithPredictions'").

- [ ] **Step 3: Implement**

```ts
// apps/web/src/hooks/useFixtureWithPredictions.ts
'use client';

import { useEffect, useState } from 'react';
import type { FixtureWithMatches, Prediction } from '@prode/shared';
import { fixtures, pronosticos } from '@/lib/endpoints';

export interface UseFixtureWithPredictionsResult {
  fixture: FixtureWithMatches | null;
  predictions: Prediction[];
  isLoading: boolean;
  error: unknown;
}

/**
 * Carga una fecha (fixture) junto con las predicciones del usuario.
 * Un fallo al cargar predicciones degrada a lista vacía (no es error fatal);
 * un fallo al cargar el fixture sí se expone como error.
 */
export function useFixtureWithPredictions(
  fechaId: string,
): UseFixtureWithPredictionsResult {
  const [fixture, setFixture] = useState<FixtureWithMatches | null>(null);
  const [predictions, setPredictions] = useState<Prediction[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<unknown>(null);

  useEffect(() => {
    let cancelled = false;
    setIsLoading(true);
    setError(null);
    Promise.all([
      fixtures.one(fechaId),
      pronosticos.byFixture(fechaId).catch(() => [] as Prediction[]),
    ])
      .then(([fx, ps]) => {
        if (cancelled) return;
        setFixture(fx);
        setPredictions(ps as Prediction[]);
      })
      .catch((e) => {
        if (!cancelled) setError(e);
      })
      .finally(() => {
        if (!cancelled) setIsLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, [fechaId]);

  return { fixture, predictions, isLoading, error };
}
```

- [ ] **Step 4: Run to confirm pass**

Run (desde apps/web): `pnpm exec vitest run src/hooks/useFixtureWithPredictions.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/hooks/useFixtureWithPredictions.ts apps/web/src/hooks/useFixtureWithPredictions.test.ts
git commit -m "feat(web): hook useFixtureWithPredictions (DRY de páginas Prode)"
```

---

## Task 2: Keys i18n de accesibilidad para MatchCard

**Files:**
- Modify: `apps/web/src/messages/{es,en,de,fr}/prode.json`

Agregar dentro del objeto `match` (junto a `status`, `scoreBonus`, `captainCheck`) las keys de aria-label. Cuidar comas JSON; validar que el JSON quede bien formado.

- [ ] **Step 1: es/prode.json** — dentro de `"match": { ... }` agregar las 3 keys de marcador/resultado, y dentro de `"tabs": { ... }` agregar `"aria"`:
```json
    "homeScoreLabel": "Goles de {team}",
    "awayScoreLabel": "Goles de {team}",
    "pickAria": { "home": "Gana el local", "draw": "Empate", "away": "Gana el visitante" }
```
```json
    "aria": "Filtrar pronósticos"
```

- [ ] **Step 2: en/prode.json** — dentro de `"match"` y dentro de `"tabs"`:
```json
    "homeScoreLabel": "{team} goals",
    "awayScoreLabel": "{team} goals",
    "pickAria": { "home": "Home win", "draw": "Draw", "away": "Away win" }
```
```json
    "aria": "Filter predictions"
```

- [ ] **Step 3: de/prode.json** — dentro de `"match"` y dentro de `"tabs"`:
```json
    "homeScoreLabel": "Tore von {team}",
    "awayScoreLabel": "Tore von {team}",
    "pickAria": { "home": "Heimsieg", "draw": "Unentschieden", "away": "Auswärtssieg" }
```
```json
    "aria": "Tipps filtern"
```

- [ ] **Step 4: fr/prode.json** — dentro de `"match"` y dentro de `"tabs"`:
```json
    "homeScoreLabel": "Buts de {team}",
    "awayScoreLabel": "Buts de {team}",
    "pickAria": { "home": "Victoire à domicile", "draw": "Match nul", "away": "Victoire à l'extérieur" }
```
```json
    "aria": "Filtrer les pronostics"
```

- [ ] **Step 5: Verificar paridad de mensajes**

Run (desde apps/web): `pnpm exec vitest run src/i18n/messages-parity.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/messages
git commit -m "feat(web): keys i18n de a11y para MatchCard (marcador y resultado)"
```

---

## Task 3: Countdown — migrar tokens

**Files:**
- Modify: `apps/web/src/components/prode/Countdown.tsx`

- [ ] **Step 1: Reemplazar el componente completo** por:

```tsx
'use client';

import { useTranslations } from 'next-intl';
import { useCountdown } from '@/hooks/useCountdown';

export function Countdown({ targetDate }: { targetDate: Date }) {
  const t = useTranslations('prode.countdown');
  const { days, hours, minutes, seconds, expired } = useCountdown(targetDate);

  if (expired) {
    return <span className="text-destructive font-medium">{t('closed')}</span>;
  }

  return (
    <span className="font-mono text-sm text-neon">
      {days > 0 && `${days}d `}
      {String(hours).padStart(2, '0')}:{String(minutes).padStart(2, '0')}:
      {String(seconds).padStart(2, '0')}
    </span>
  );
}
```

- [ ] **Step 2: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/prode/Countdown.tsx
git commit -m "fix(web): Countdown usa tokens neon/destructive"
```

---

## Task 4: MatchCard — responsive 360px + accesibilidad

**Files:**
- Modify: `apps/web/src/components/prode/MatchCard.tsx`

MatchCard ya usa tokens nuevos. Cambios: hacerlo caber en 360px y agregar ARIA. NO cambiar la lógica de `setResult`/`setScore`/`onChange`.

- [ ] **Step 1: Reducir tamaños en mobile (responsive).**

En el contenedor `<div className="p-5">` (línea ~103), cambiar `p-5` por `p-4 sm:p-5`.

En el contenedor de los 3 botones de resultado, cambiar `<div className="flex items-center gap-2">` (línea ~142) por `<div className="flex items-center gap-1.5 sm:gap-2">`.

En cada botón de resultado, cambiar `'size-12 rounded-lg ...'` (línea ~152) por `'size-10 sm:size-12 rounded-lg ...'` (mantener el resto de las clases idénticas).

En el contenedor exterior de equipos, cambiar `<div className="flex items-center justify-between gap-4">` (línea ~124) por `<div className="flex items-center justify-between gap-2 sm:gap-4">`.

En ambos `<TeamFlag size="lg" ... />` (home línea ~127 y away línea ~166), cambiar a `size="md"` y agregar `className="sm:size-12"` NO — `TeamFlag` no acepta override fiable; en su lugar dejar `size="lg"` (48px) PERO envolver el nombre con `truncate`: en los dos `<p>` de nombre de equipo (líneas ~131 y ~171) agregar `max-w-full truncate` a la clase existente, y agregar `min-w-0` a las dos columnas `flex-1 flex flex-col items-center gap-2` (líneas ~125 y ~165) → `flex-1 min-w-0 flex flex-col items-center gap-2`. Esto evita desborde sin achicar las banderas.

- [ ] **Step 2: a11y de los botones 1/X/2.**

`MatchCard` ya hace `const t = useTranslations('prode.match');`. En el `.map` de `RESULT_LABELS` (líneas ~143-162), en el `<button>` agregar `aria-pressed={active}` y `aria-label`. El mapeo de `Result` a key de aria: definir arriba del componente, junto a `RESULT_LABELS`:

```tsx
const RESULT_ARIA_KEY: Record<Result, string> = {
  [Result.HOME]: 'home',
  [Result.DRAW]: 'draw',
  [Result.AWAY]: 'away',
};
```

Y el botón queda:
```tsx
                <button
                  key={r}
                  type="button"
                  disabled={isLocked}
                  aria-pressed={active}
                  aria-label={t(`pickAria.${RESULT_ARIA_KEY[r]}`)}
                  onClick={() => setResult(r)}
                  className={cn(
                    'size-10 sm:size-12 rounded-lg font-display font-extrabold text-lg transition-all',
                    active
                      ? 'bg-neon text-primary-foreground glow-neon'
                      : 'bg-surface-2 text-foreground hover:bg-surface-3',
                    isLocked ? 'cursor-not-allowed' : 'active:scale-90',
                  )}
                >
                  {RESULT_LABELS[r]}
                </button>
```

- [ ] **Step 3: a11y de los inputs de marcador.**

En los dos `<input>` de marcador (líneas ~189-205), agregar `aria-label`, `inputMode="numeric"`. El input "home":
```tsx
              <input
                className="w-10 h-8 bg-surface-2 rounded text-center text-sm font-display font-bold focus:ring-1 focus:ring-neon p-0 tabular-nums"
                type="number"
                inputMode="numeric"
                min={0}
                placeholder="0"
                aria-label={t('homeScoreLabel', { team: teamName(match, 'home') })}
                value={pick?.homeScoreGuess ?? ''}
                onChange={(e) => setScore('home', e.target.value)}
              />
```
El input "away" igual pero `aria-label={t('awayScoreLabel', { team: teamName(match, 'away') })}` y `onChange={(e) => setScore('away', e.target.value)}` con `value={pick?.awayScoreGuess ?? ''}`.

- [ ] **Step 4: focus visible en el checkbox de capitán.**

En el `<input type="checkbox">` (líneas ~213-219), agregar a su className (hoy sin className) `className="accent-neon focus-visible:ring-2 focus-visible:ring-neon focus-visible:outline-none"`.

- [ ] **Step 5: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores. (Las keys `pickAria.*`/`homeScoreLabel`/`awayScoreLabel` ya existen por Task 2.)

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/prode/MatchCard.tsx
git commit -m "feat(web): MatchCard responsive 360px y accesible (aria-pressed, labels de marcador)"
```

---

## Task 5: PredictionsProgressCard — adoptar PercentBar

**Files:**
- Modify: `apps/web/src/components/prode/PredictionsProgressCard.tsx`

- [ ] **Step 1: Reemplazar la barra manual por PercentBar.**

Agregar el import:
```tsx
import { PercentBar } from '@/components/ui/percent-bar';
```
Reemplazar el bloque de barra (el `<div className="mt-3 h-2 ...">...</div>`, líneas ~33-38) por:
```tsx
        <PercentBar
          value={predicted}
          max={total}
          tone="neon"
          label={t('percent', { pct })}
          className="mt-3"
        />
```
(El resto del componente —título, subtítulo, ícono, fila pending/percent— no cambia.)

- [ ] **Step 2: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/prode/PredictionsProgressCard.tsx
git commit -m "refactor(web): PredictionsProgressCard usa PercentBar (role=progressbar)"
```

---

## Task 6: PredictionsFilterTabs — adoptar PillTabs

**Files:**
- Modify: `apps/web/src/components/prode/PredictionsFilterTabs.tsx`

Mantener la API pública (`value`, `onChange`, `counts`) para no tocar el consumidor (`prode/page.tsx`). Por dentro, derivar los tabs y delegar en `PillTabs` (que aporta `role="tablist"/"tab"`, `aria-selected`, scroll-x y foco).

- [ ] **Step 1: Reemplazar el componente completo** por:

```tsx
'use client';

import { useTranslations } from 'next-intl';
import { PillTabs, type PillTab } from '@/components/ui/pill-tabs';

export type PredictionsFilter =
  | 'pending'
  | 'predicted'
  | 'live'
  | 'results'
  | 'topScorer';

const ORDER: PredictionsFilter[] = [
  'pending',
  'predicted',
  'live',
  'results',
  'topScorer',
];

interface Props {
  value: PredictionsFilter;
  onChange: (v: PredictionsFilter) => void;
  counts?: Partial<Record<PredictionsFilter, number>>;
}

export function PredictionsFilterTabs({ value, onChange, counts }: Props) {
  const t = useTranslations('prode.tabs');
  const tabs: PillTab<PredictionsFilter>[] = ORDER.map((k) => ({
    value: k,
    label: t(k),
    count: counts?.[k],
  }));

  return (
    <div className="-mx-4 px-4">
      <PillTabs
        tabs={tabs}
        value={value}
        onValueChange={onChange}
        aria-label={t('aria')}
      />
    </div>
  );
}
```

Nota: `PillTabs` aplica `text-xs font-black uppercase tracking-widest`; es coherente con el resto de tabs de la app. El wrapper `-mx-4 px-4` conserva el scroll full-bleed dentro del contenedor `px-4` de la página.

- [ ] **Step 2: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/components/prode/PredictionsFilterTabs.tsx
git commit -m "refactor(web): PredictionsFilterTabs usa PillTabs accesible"
```

---

## Task 7: ProdeForm — migrar tokens + a11y + PercentBar

**Files:**
- Modify: `apps/web/src/components/prode/ProdeForm.tsx`

Componente con mucho legacy. Migrar tokens, adoptar `PercentBar` en la barra de progreso, y agregar ARIA a mensajes y al `<select>`. NO cambiar la lógica de guardado.

- [ ] **Step 1: Agregar import de PercentBar.**

Junto a los imports de componentes (después de `import { Countdown } from './Countdown';`):
```tsx
import { PercentBar } from '@/components/ui/percent-bar';
```

- [ ] **Step 2: Migrar el bloque de progreso (cabecera).**

Reemplazar el bloque `<div className="rounded-xl p-4 ..." style={{ background: 'var(--surface-container-low)' }}> ... </div>` (líneas ~166-198) por:
```tsx
      <div className="rounded-xl p-4 flex items-center justify-between gap-4 bg-surface-1">
        <div>
          <p className="text-xs uppercase tracking-widest text-ink-muted font-bold">
            {isKnockoutFixture ? t('nextClose') : t('closesIn')}
          </p>
          {nextOpenDeadline ? (
            <Countdown targetDate={nextOpenDeadline} />
          ) : (
            <p className="text-sm font-bold text-ink-muted">{t('closed')}</p>
          )}
        </div>
        <div className="text-right min-w-0">
          <p className="text-xs uppercase tracking-widest text-ink-muted font-bold">
            {t('progress')}
          </p>
          <p className="text-sm font-bold text-foreground">
            {t('progressValue', { filled: filledCount, total: totalMatches })}
          </p>
          <PercentBar
            value={filledCount}
            max={totalMatches}
            tone="neon"
            label={t('progressValue', { filled: filledCount, total: totalMatches })}
            className="mt-1 w-32 sm:w-40 ml-auto"
          />
        </div>
      </div>
```

- [ ] **Step 3: Migrar el bloque del capitán + asociar label al select.**

Reemplazar el bloque `{!allClosed && !isKnockoutFixture && (<div ... style={{ background: 'var(--surface-container-low)' }}>...)}` (líneas ~200-226) por:
```tsx
      {!allClosed && !isKnockoutFixture && (
        <div className="rounded-xl p-4 flex items-center justify-between gap-4 bg-surface-1">
          <div>
            <p
              id="captain-label"
              className="text-xs uppercase tracking-widest text-ink-muted font-bold"
            >
              {t('captainTitle')}
            </p>
            <p className="text-sm font-bold text-foreground">{t('captainDesc')}</p>
          </div>
          <select
            aria-labelledby="captain-label"
            className="bg-surface-2 text-foreground text-sm rounded-lg py-2 px-3 font-medium focus-visible:ring-2 focus-visible:ring-neon focus-visible:outline-none"
            value={captainId ?? ''}
            onChange={(e) => setCaptainId(e.target.value || null)}
          >
            <option value="">{t('noCaptain')}</option>
            {fixture.matches.map((m) => (
              <option key={m.id} value={m.id}>
                {m.homeTeamName} vs {m.awayTeamName}
              </option>
            ))}
          </select>
        </div>
      )}
```

- [ ] **Step 4: Migrar el botón "guardar este".**

En el botón dentro del `.map` (líneas ~242-251), cambiar la clase `text-primary` por `text-neon`:
```tsx
                  <button
                    type="button"
                    onClick={() => savePick(match.id)}
                    disabled={savingMatchId === match.id}
                    className="text-xs font-bold text-neon hover:underline disabled:opacity-50"
                  >
                    {savingMatchId === match.id ? t('saving') : t('saveThis')}
                  </button>
```

- [ ] **Step 5: Migrar mensajes (tokens + ARIA) y el botón "guardar todos".**

Reemplazar el bloque de mensajes + botón saveAll (líneas ~259-274) por:
```tsx
      {submitError && (
        <p role="alert" className="text-sm font-bold text-destructive">
          {submitError}
        </p>
      )}
      {successMsg && (
        <p aria-live="polite" className="text-sm font-bold text-neon">
          {successMsg}
        </p>
      )}

      {!allClosed && (
        <button
          type="button"
          onClick={saveAll}
          className="w-full h-14 bg-neon text-primary-foreground font-extrabold text-lg rounded-xl active:scale-[0.98] transition-transform"
        >
          {t('saveAll')}
        </button>
      )}
```

- [ ] **Step 6: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores. Verificar con grep que no quedan tokens legacy en el archivo:
`grep -nE "surface-container|on-surface-variant|text-white|text-red-|bg-primary |text-black|text-primary\b" apps/web/src/components/prode/ProdeForm.tsx` → debe devolver vacío.

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/components/prode/ProdeForm.tsx
git commit -m "feat(web): ProdeForm con tokens nuevos, PercentBar y a11y (role=alert, select etiquetado)"
```

---

## Task 8: prode/page.tsx — enum MatchStatus + formato de fecha

**Files:**
- Modify: `apps/web/src/app/(main)/prode/page.tsx`

NO tocar la lógica del filtro (Fase 7). Solo: usar el enum `MatchStatus` en vez de strings mágicos, y centralizar el formato de deadline con `lib/date`.

- [ ] **Step 1: Imports.**

Agregar a los imports de `@prode/shared` el enum y traer `useLocale` y `formatDeadline`:
- Cambiar `import type { FixtureWithMatches } from '@prode/shared';` por:
```tsx
import type { FixtureWithMatches } from '@prode/shared';
import { MatchStatus } from '@prode/shared';
```
- Agregar a `next-intl`: la página ya importa `useTranslations`; agregar `useLocale`:
```tsx
import { useTranslations, useLocale } from 'next-intl';
```
- Agregar:
```tsx
import { formatDeadline } from '@/lib/date';
```

- [ ] **Step 2: Eliminar la función local `formatDeadline`.**

Borrar la función `function formatDeadline(date: string | null): string { ... }` (líneas ~35-45) por completo (se reemplaza por la de `lib/date`).

- [ ] **Step 3: Usar el locale al formatear.**

Dentro del componente `ProdePage`, después de `const tFeat = useTranslations('prode.featured');`, agregar:
```tsx
  const locale = useLocale();
```
Y donde se calcula `deadlineLabel` (línea ~102), cambiar:
```tsx
  const deadlineLabel = formatDeadline(tournament?.startDate ?? null, locale);
```

- [ ] **Step 4: Usar el enum en los conteos.**

En el cálculo de `live`/`finished` (líneas ~91-92), cambiar los literales por el enum:
```tsx
  const live = allMatches.filter((m) => m.status === MatchStatus.LIVE).length;
  const finished = allMatches.filter((m) => m.status === MatchStatus.FINISHED).length;
```

- [ ] **Step 5: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/app/\(main\)/prode/page.tsx
git commit -m "refactor(web): prode/page usa enum MatchStatus y lib/date"
```

---

## Task 9: prode/[fechaId]/page.tsx — tokens + hook + a11y

**Files:**
- Modify: `apps/web/src/app/(main)/prode/[fechaId]/page.tsx`

- [ ] **Step 1: Reemplazar el archivo completo** por (usa el hook nuevo, tokens nuevos, header alineado con `resultados`, `role="alert"` y `Skeleton`):

```tsx
'use client';

import { use } from 'react';
import Link from 'next/link';
import { useTranslations } from 'next-intl';
import { ProdeForm } from '@/components/prode/ProdeForm';
import { Skeleton } from '@/components/ui/skeleton';
import { useFixtureWithPredictions } from '@/hooks/useFixtureWithPredictions';

export default function ProdeFechaPage({
  params,
}: {
  params: Promise<{ fechaId: string }>;
}) {
  const t = useTranslations('prode');
  const { fechaId } = use(params);
  const { fixture, predictions, isLoading, error } =
    useFixtureWithPredictions(fechaId);

  if (error) {
    return (
      <main className="pt-24 pb-12 px-4 max-w-3xl mx-auto">
        <p role="alert" className="text-sm text-destructive font-bold">
          {error instanceof Error ? error.message : t('fixture.loadError')}
        </p>
        <Link href="/prode" className="text-neon underline text-sm">
          {t('fixture.backToListLong')}
        </Link>
      </main>
    );
  }

  if (isLoading || !fixture) {
    return (
      <main className="pt-24 pb-12 px-4 max-w-3xl mx-auto">
        <Skeleton className="h-64 w-full rounded-2xl" />
      </main>
    );
  }

  return (
    <main className="pt-24 pb-32 px-4 max-w-3xl mx-auto">
      <Link
        href="/prode"
        className="text-[10px] font-display font-bold text-ink-muted hover:text-neon uppercase tracking-[0.18em]"
      >
        {t('fixture.backToList')}
      </Link>
      <header className="mt-3 mb-6">
        <p className="font-display text-xs uppercase tracking-[0.2em] text-neon mb-2">
          {t('list.eyebrow')}
        </p>
        <h1 className="font-display font-extrabold text-4xl text-foreground tracking-tight">
          {fixture.name ?? t('fixture.fallbackName', { round: fixture.round })}
        </h1>
        <Link
          href={`/prode/${fixture.id}/resultados`}
          className="inline-block mt-2 text-sm text-neon hover:underline"
        >
          {t('fixture.viewResults')}
        </Link>
      </header>

      <ProdeForm fixture={fixture} initialPredictions={predictions} />
    </main>
  );
}
```

- [ ] **Step 2: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores. Verificar sin legacy:
`grep -nE "surface-container|on-surface-variant|text-white|text-red-|text-primary\b" apps/web/src/app/\(main\)/prode/\[fechaId\]/page.tsx` → vacío.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app/\(main\)/prode/\[fechaId\]/page.tsx
git commit -m "feat(web): prode/[fechaId] usa hook compartido, tokens nuevos y a11y"
```

---

## Task 10: resultados/page.tsx — hook + responsive + empty

**Files:**
- Modify: `apps/web/src/app/(main)/prode/[fechaId]/resultados/page.tsx`

Ya usa tokens nuevos. Cambios: adoptar el hook (DRY), evitar desborde de nombres en 360px, y mostrar estado vacío si no hay partidos.

- [ ] **Step 1: Adoptar el hook compartido.**

Reemplazar los imports de fetch + el bloque de estado/effect. Quitar:
```tsx
import { fixtures, pronosticos } from '@/lib/endpoints';
```
y los `useState`/`useEffect` de `fixture`/`preds`/`error` (líneas ~42-56). En su lugar, dejar el import:
```tsx
import { useFixtureWithPredictions } from '@/hooks/useFixtureWithPredictions';
```
y dentro del componente, reemplazar la declaración de estado + effect por:
```tsx
  const { fechaId } = use(params);
  const { fixture, predictions: preds, isLoading, error } =
    useFixtureWithPredictions(fechaId);
```
(Mantener `useMemo` de `totalPoints` usando `preds`, y `use`/`useMemo` en los imports de react; quitar `useState`/`useEffect` del import de react si quedan sin uso.)

- [ ] **Step 2: Ajustar el guard de carga.**

Cambiar `if (!fixture) {` (línea ~71) por `if (isLoading || !fixture) {`. Y en el bloque de error, anteponer `role="alert"`:
```tsx
        <p role="alert" className="text-sm text-destructive font-bold">{error instanceof Error ? error.message : t('fixture.loadError')}</p>
```

- [ ] **Step 3: Estado vacío.**

Después del `const predByMatch = ...` y antes del `return (`, agregar import de Empty arriba:
```tsx
import { Empty, EmptyHeader, EmptyTitle, EmptyDescription } from '@/components/ui/empty';
```
y dentro del `return`, envolver: si `fixture.matches.length === 0`, en lugar del `<ul>` renderizar el Empty. Concretamente, reemplazar el `<ul className="space-y-3"> ... </ul>` por:
```tsx
      {fixture.matches.length === 0 ? (
        <Empty>
          <EmptyHeader>
            <EmptyTitle>{t('list.emptyTitle')}</EmptyTitle>
            <EmptyDescription>{t('list.emptyDesc')}</EmptyDescription>
          </EmptyHeader>
        </Empty>
      ) : (
        <ul className="space-y-3">
          {/* ...contenido existente del map... */}
        </ul>
      )}
```
(Mantener el contenido del `.map` tal cual dentro del `<ul>`.)

- [ ] **Step 4: Responsive de la fila de equipos (evitar desborde a 360px).**

En el bloque de la fila superior de cada card (líneas ~125-138), cambiar el contenedor izquierdo `<div className="flex items-center gap-3">` por `<div className="flex items-center gap-2 min-w-0 flex-1">` y agregar `truncate` a los dos `<span>` de nombre de equipo (home y away) → `className="font-display font-bold text-sm text-foreground truncate"`. Además, al `<span>` del marcador/pending de la derecha agregar `shrink-0`:
```tsx
                  <span className="shrink-0 text-xs uppercase tracking-[0.18em] font-display font-bold text-ink-muted tabular-nums">
```

- [ ] **Step 5: Verificar build**

Run (desde apps/web): `pnpm build`
Expected: compila sin errores.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/app/\(main\)/prode/\[fechaId\]/resultados/page.tsx
git commit -m "feat(web): resultados usa hook compartido, responsive y estado vacío"
```

---

## Task 11: Verificación final de la Fase 1

**Files:** ninguno (verificación).

- [ ] **Step 1: Suite completa de tests.**

Run (desde apps/web): `pnpm test`
Expected: todos verdes (incluye los nuevos de `useFixtureWithPredictions` y la paridad i18n).

- [ ] **Step 2: Build de producción.**

Run (desde apps/web): `pnpm build`
Expected: 0 errores de tipo, compila.

- [ ] **Step 3: Sin tokens legacy en el dominio Prode.**

Run (desde la raíz):
```bash
grep -rnE "surface-container|on-surface-variant|text-white|text-red-[0-9]|accent-primary|bg-primary text-black" apps/web/src/components/prode apps/web/src/app/\(main\)/prode
```
Expected: vacío (0 coincidencias).

- [ ] **Step 4: Revisión visual (controlador + usuario).**

Levantar `pnpm dev` y verificar en 360px y desktop:
- `/prode`: tabs de filtro scrollean y se ven como píldoras; barra de progreso visible.
- `/prode/[fechaId]`: MatchCard no se desborda a 360px; botones 1/X/2 a `size-10`; inputs de marcador usables; guardar funciona; mensajes de error/éxito aparecen.
- `/prode/[fechaId]/resultados`: nombres largos no desbordan (truncan); estado vacío si no hay partidos.
- Sin scroll horizontal de página en ninguna.

Documentar y corregir cualquier ajuste antes de cerrar la fase.

---

## Cierre de la Fase 1

Al terminar:
- Hook `useFixtureWithPredictions` con tests; las dos páginas de fecha lo usan (sin duplicación).
- Dominio Prode 100% en tokens nuevos (0 legacy), responsive a 360px y con ARIA (aria-pressed, labels de marcador, role=alert, tabs accesibles, progressbar).
- `PillTabs` y `PercentBar` adoptados.

**Siguiente:** Fase 2 — Landing + Auth (incluye migrar Material Symbols → lucide, que arregla los íconos-como-texto de la landing).
